{
  "name": "nori-os-offline",
  "version": "1.0.0",
  "private": true,
  "description": "NoriOS offline archive + local backend for the public NoriOS web client (Pure HTML5 with Cordis)",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "start:offline": "node server.mjs --offline",
    "check": "node --check server.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@11.24.0",
  "dependencies": {}
}
