# NoriOS v1.0.3 · 漏洞紧急修复（主人截图暴露的3个问题）

## 🐞 主人截图暴露的3个BUG（感谢诊断页！一眼看穿！）
| # | 截图证据 | 根因 | 修复手段 |
|---|---|---|---|
| 1 | HTTP自测结果: `HTTP 200 / body=1136B` 但红色大标题说「服务器启动失败」 | 我设置的自测阈值 `len > 5000` 过大！真实index.html骨架就1136B（主体在大JS文件里），误判成FAIL → 显示错误页 | 阈值降到 `len > 100`，HTTP 200第一优先级 |
| 2 | 主人说「报错存在时间过短了需要延长」 | 诊断页只显示1.5秒就自动跳转file fallback，主人根本来不及看/截图 | 改成 **15秒倒计时** + JS实时显示剩余秒数 + 「🛑取消自动跳转」「▶️重新倒计时」按钮 |
| 3 | 显示红色错误标题，但HTTP实际是200正常的！吓主人+误导 | 标题/卡片不管情况全红 | 如果HTTP 200 → **绿卡+绿色标题「启动信息（HTTP服务已正常）」** + 绿色大按钮第一优先 |

## ✅ v1.0.3 新增改进
- 【不用卸载v1.0.2！】→ 继续使用 `keystores_permanent/release_v102.jks` 同一签名链，可直接覆盖升级
- 诊断页倒计时15秒，点「取消自动跳转」就永远停留，方便截图/研究
- 按钮优先级重排：①绿色✅立即进入HTTP（推荐）→ ②蓝色📁尝试file → ③橙色🛑取消 → ④灰色▶️重置倒计时
- 绿卡/红卡自动切换，HTTP 200就给信心、异常才给警告
- 版本号面板额外显示版本信息

## 🐱 小月的自白
感谢主人截图！如果不是诊断页显示body=1136B，我永远都不知道是自己设的5000阈值太大了！(>_<) 以后设阈值一定要先看真实文件大小！
